user             nginx;
worker_processes auto;

worker_rlimit_nofile 60000;

error_log /var/log/nginx/error.log warn;
pid       /var/run/nginx.pid;

events {
    worker_connections 30000;
    multi_accept       on;
}

http {
    include      mime.types;
    default_type application/octet-stream;

    sendfile    on;
    tcp_nopush  on;
    tcp_nodelay on;

    keepalive_timeout  10s;
    keepalive_requests 200;

    upstream streamkey_api {
        server    streamkey-api:5555;
        keepalive 32;
    }

    upstream seq_ui {
        server    seq:80;
        keepalive 16;
    }

    access_log off;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" ' '$status $body_bytes_sent "$http_referer" ' '"$http_user_agent" "$http_x_forwarded_for"';

    # ------------------------
    # Redirect HTTP → HTTPS
    # ------------------------
    server {
        listen      80  default_server;
        server_name _;
        return      301 https://service.streamkey.ru$request_uri;
    }

    # ------------------------
    # HTTPS server
    # ------------------------
    server {
        listen      443                  ssl http2;
        server_name service.streamkey.ru;

        ssl_certificate     /etc/letsencrypt/live/service.streamkey.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/service.streamkey.ru/privkey.pem;

        ssl_protocols       TLSv1.2          TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;
        ssl_session_cache   shared:SSL:20m;
        ssl_session_timeout 10m;

        add_header Strict-Transport-Security "max-age=31536000" always;

        # ------------------------
        # API
        # ------------------------
        location / {
            proxy_pass         http://streamkey_api;
            proxy_http_version 1.1;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Authorization     $http_authorization;

            proxy_set_header Connection "close";

            proxy_pass_request_headers on;
            proxy_redirect             off;

            proxy_connect_timeout 3s;
            proxy_send_timeout    15s;
            proxy_read_timeout    30s;
        }

        # ------------------------
        # Static files
        # ------------------------
        location /files/ {
            alias     /app/files/;
            try_files $uri        $uri/ =404;

            expires    1d;
            add_header Cache-Control "public, immutable";
        }

        # ------------------------
        # SignalR WebSocket
        # ------------------------
        location /hubs/extension {
            proxy_pass         http://streamkey-api:5555/hubs/extension;
            proxy_http_version 1.1;

            proxy_set_header Upgrade           $http_upgrade;
            proxy_set_header Connection        "Upgrade";
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 3s;
            proxy_read_timeout    120s;
            proxy_send_timeout    120s;
        }

        # ------------------------
        # SEQ UI
        # ------------------------
        location /seq/ {
            proxy_pass         http://seq:80/;
            proxy_http_version 1.1;

            proxy_set_header Upgrade           $http_upgrade;
            proxy_set_header Connection        "Upgrade";
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 3s;
            proxy_read_timeout    60s;
            proxy_send_timeout    60s;

            proxy_redirect off;

            rewrite ^/seq(/.*)$ $1 break;
        }
    }
}
